"""Dev-only movement-calibration analysis scripts. All resolve their CSV
inputs/outputs CWD-relative; invoke from the repository root, unchanged
from before this package existed. Calibration DATA files are scientific
artifacts and are never moved into this package -- they remain at the
repository root."""
