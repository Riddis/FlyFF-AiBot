# Python caches
__pycache__/
*.py[cod]
.pytest_cache/
.pytest_tmp/
.ruff_cache/

# Runtime diagnostics and generated mapping output
debug/
mapper/mapping_runs/
gui_crash.log

# Machine-specific online learning state
mapper/adaptive_motion.json
mapper/adaptive_motion.invalid_*.json

# Temporary archives produced during development
mapper.zip
foreground_vision_bot.zip
refactor_logs.zip

# Persistent learned map data (keep locally; map_profiles.json is tracked)
mapper/maps/

# RL model and TensorBoard output (folder layout remains tracked)
models/farming/*
!models/farming/.gitkeep
models/mapping/*
!models/mapping/.gitkeep
training_logs/farming/*
!training_logs/farming/.gitkeep
training_logs/mapping/*
!training_logs/mapping/.gitkeep
training_logs/movement/

# One-time layout repair backups
migration_backups/
